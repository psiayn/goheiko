nodes:
  - name: node1
    host: IP
    username: ssh_username
    port: 22
    auth:
      method: keys
      keys:
        path: /path/to/keys
  - name: node2
    host: IP
    username: ssh_username
    port: 8022
    auth:
      method: keys
  - name: node3
    host: IP
    username: ssh_username
    port: 8022
    auth:
      method: password
      password: ssh_password
  - name: node4
    host: IP
    username: ssh_username
    port: 22

jobs:
  - name: job_1
    init:
      - "touch init.txt"
    commands:
      - "cd ~/Downloads"
      - "touch sample.txt"
  - name: job_2
    init:
      - "touch init.txt"
    commands:
      - "cd ~/Downloads"
      - "touch sample.txt"
  - name: job_3
    init:
      - "touch init.txt"
    commands:
      - "cd ~/Downloads"
      - "touch sample.txt"
  - name: job_4
    init:
      - "touch init.txt"
    commands:
      - "cd ~/Downloads"
      - "touch sample.txt"
  - name: job_5
    init:
      - "touch init.txt"
    commands:
      - "cd ~/Downloads"
      - "touch sample.txt"
  - name: job_6
    init:
      - "touch init.txt"
    commands:
      - "cd ~/Downloads"
      - "touch sample.txt"
